/**
 * Supabase Client — DISABLED
 * Auth and data have been migrated to JWT + FastAPI backend.
 * This file is kept as a stub to avoid breaking any stale imports during transition.
 */

// No-op stub — nothing should import this anymore
export const supabase = null as any;
export default supabase;
